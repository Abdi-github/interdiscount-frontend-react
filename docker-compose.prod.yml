services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: interdiscount_frontend_react_prod
    restart: unless-stopped
    expose:
      - "80"
    environment:
      - NODE_ENV=production
    networks:
      - interdiscount_network
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.docker.allownonrunning=true"
      - "traefik.http.routers.interdiscount-react.rule=Host(`interdiscount-react.swiftapp.ch`)"
      - "traefik.http.routers.interdiscount-react.entrypoints=websecure"
      - "traefik.http.routers.interdiscount-react.tls.certresolver=letsencrypt"
      - "traefik.http.routers.interdiscount-react.middlewares=sablier-interdiscount@file"
      - "traefik.http.services.interdiscount-react.loadbalancer.server.port=80"
      - "sablier.enable=true"
      - "sablier.group=interdiscount"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

networks:
  interdiscount_network:
    external: true
  traefik:
    external: true
